{
  "mcpServers": {
    "adguard": {
      "command": "adguard-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "ADGUARD_URL": "${ADGUARD_URL:-http://localhost:8080}",
        "ADGUARD_TOKEN": "${ADGUARD_TOKEN}"
      }
    }
  }
}
